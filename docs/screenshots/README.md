# Blog screenshots

Drop your Medium blog screenshots here, then update the placeholders in `BLOG.md`.

| Placeholder in BLOG.md | Suggested filename | What to capture |
|------------------------|--------------------|-----------------|
| Dashboard              | `dashboard.png`        | Main grid with varied status pills + origin badges |
| Needs attention        | `needs-attention.png`  | "Needs attention" filter on + notification popup |
| Session detail         | `session-detail.png`   | Detail header, "what's expected" summary, chat box |
| Search                 | `search.png`           | A wildcard query (e.g. `*docker*`) and results |

The architecture diagram is already rendered at `docs/architecture.png`
(source: `docs/architecture.dot`, re-render with
`dot -Tpng -Gdpi=150 docs/architecture.dot -o docs/architecture.png`).
